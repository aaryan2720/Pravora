export const metadata = {
  title: 'Restaurant Sign In',
  description: 'Sign in to your Pravora restaurant management dashboard to control live tables, kitchen orders, and operations.',
  alternates: {
    canonical: '/auth/signin',
  },
};

export default function SignInLayout({ children }) {
  return children;
}
