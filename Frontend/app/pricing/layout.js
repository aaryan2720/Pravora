export const metadata = {
  title: 'Pricing Plans',
  description: 'Simple, transparent pricing plans for modern restaurants, cafes, and multi-branch food chains.',
  alternates: {
    canonical: '/pricing',
  },
};

export default function PricingLayout({ children }) {
  return children;
}
