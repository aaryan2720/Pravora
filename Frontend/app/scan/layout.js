export const metadata = {
  title: 'Scan Table QR',
  description: 'Point your camera at the table QR code to start live ordering and view real-time menu items.',
  alternates: {
    canonical: '/scan',
  },
};

export default function ScanLayout({ children }) {
  return children;
}
